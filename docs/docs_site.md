# Docs Site

Project documentation is served with MkDocs Material.

## Install docs dependencies

```bash
pip install -r requirements-docs.txt
```

## Run locally

```bash
mkdocs serve
```

## Build static docs

```bash
mkdocs build
```

The documentation sources live in `docs/`, and the site configuration lives in `mkdocs.yml`.
